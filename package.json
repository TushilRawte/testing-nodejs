{
  "name": "nodejs_practice_3",
  "version": "1.0.0",
  "description": "this is practice session for testing api",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install npm && run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tushil",
  "license": "ISC",
    "engines": {
    "node": "18.14.2"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
